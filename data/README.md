# Data

`data/raw/` is ignored and contains the authorized FD001 training/test/truth files only for validation and retraining. It is excluded from Vercel and not needed for prediction runtime. `data/sample/` contains small deterministic synthetic development fixtures. Official source provenance and archive SHA-256 are recorded in `docs/data/00-official-dataset-provenance.md`.
