# Canonical application correction

The supported product is a browser website backed by stateless FastAPI, not a local launcher. Ordinary Uvicorn commands support development. Streamlit is an optional HTTP client only; legacy frontend/backend folders are preserved and excluded from the canonical runtime.
