# Backend release candidate

The backend is verified with a genuine FD001 real artifact and final metrics. Its public deployment boundary is now stateless FastAPI v1; the earlier dataset-registry contract is retired. `RULService` remains the sole ML boundary and demo/real artifacts remain isolated.
