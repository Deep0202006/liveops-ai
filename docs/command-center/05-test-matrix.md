# Test matrix

Backend: deterministic selection; same-input pack identity; checksum; schema; real-artifact parity; bounds; nonnegative RUL; thresholds; event deduplication; source integrity; no raw leakage; gzip budgets; catalog consistency.

Worker: load, play/pause/resume, step, reset, bidirectional seek, speeds, completion, ordering, deduplication, hidden-tab pause, rapid changes, termination, and accelerated soak.

UI: fleet observation, controls, asset selection, RUL/telemetry updates, alert focus/acknowledgement, maintenance compare/review, command palette, Data Lab valid/invalid workflows, return navigation, keyboard, reduced motion, zoom, six viewports, Chromium/Firefox/WebKit, deterministic visual baselines.
