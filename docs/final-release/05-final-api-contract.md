# Final API contract

API v1 is final for frontend implementation. Inputs are stateless multipart requests; there are no dataset IDs. Endpoints: root, health live/ready, status, model metadata/evaluation, demo sample catalogue, dataset inspection, machine inspection, machine series, and prediction. The generated source of truth is `docs/web-api/openapi-v1.json`.
