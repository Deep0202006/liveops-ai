# Final architecture

Future frontend → stateless multipart/JSON FastAPI v1 → `RULService` → validation, causal features, and versioned artifact. The API contains no ML calculations, registry, database, or cross-request state. Streamlit is optional development tooling.
