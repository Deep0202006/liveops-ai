# Lightweight web safety

The API enforces explicit CORS origins, no credentials/wildcard, CSV-only multipart, a 4 MiB byte limit, eight requested sensors, 1000 series points, safe error envelopes, and no model upload, path input, shell, eval, persistence, or authentication claims. Default local development binds to localhost.
