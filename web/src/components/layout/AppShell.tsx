import { Activity, Beaker, BrainCircuit, Menu, ShieldAlert, Wrench, X } from "lucide-react";
import { useEffect, useState, type ReactNode } from "react";
import { Link, NavLink } from "react-router-dom";

const links=[{to:"/",label:"Command Center",icon:Activity},{to:"/",label:"Fleet",icon:ShieldAlert},{to:"/maintenance",label:"Maintenance",icon:Wrench},{to:"/model",label:"Model Evidence",icon:BrainCircuit},{to:"/lab",label:"Data Lab",icon:Beaker}];
export function Wordmark(){return <Link className="wordmark" aria-label="LiveOps AI command center" to="/"><span><Activity/></span><strong>LiveOps AI</strong></Link>}
export function AppShell({children}:{children:ReactNode}){const [open,setOpen]=useState(false);useEffect(()=>{if(!open)return;const close=(event:KeyboardEvent)=>{if(event.key==="Escape")setOpen(false)};window.addEventListener("keydown",close);return()=>window.removeEventListener("keydown",close)},[open]);return <div className="operations-shell"><aside className="operations-rail"><Wordmark/><nav aria-label="Application navigation">{links.map(({to,label,icon:Icon},index)=><NavLink end={index<2} to={to} key={label} aria-label={label}><Icon/><span>{label}</span></NavLink>)}</nav><button className="mobile-nav-trigger" onClick={()=>setOpen(true)} aria-label="Open navigation"><Menu/></button></aside>{open&&<div className="modal-backdrop" onMouseDown={()=>setOpen(false)}><nav className="mobile-nav" role="dialog" aria-modal="true" aria-label="Application navigation" onMouseDown={event=>event.stopPropagation()}><button autoFocus onClick={()=>setOpen(false)} aria-label="Close navigation"><X/></button><Wordmark/>{links.map(({to,label,icon:Icon})=><NavLink to={to} key={label} onClick={()=>setOpen(false)}><Icon/>{label}</NavLink>)}</nav></div>}<main id="main-content">{children}</main></div>}
